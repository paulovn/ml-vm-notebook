Place R files here so that they are visible in both the host and the VM.

RStudio should have this folder as default; if not you can set the option in
  Tools -> Global Options -> Default working directory

